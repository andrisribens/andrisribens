# Copy to .env.local and fill in values.
# See AGENTS.md for details.

# Met.no location forecast API (no trailing ? — lat/lon appended in code)
NEXT_PUBLIC_WEATHER_API_URL=https://api.met.no/weatherapi/locationforecast/2.0/compact

# Nominatim search API (query params appended in code: city=… or q=…)
NEXT_PUBLIC_PLACE_API_URL=https://nominatim.openstreetmap.org/search?
